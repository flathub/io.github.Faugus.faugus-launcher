app-id: io.github.Faugus.faugus-launcher
sdk: org.gnome.Sdk
runtime: org.gnome.Platform
runtime-version: &runtime-version '51'
x-fd-version: &fd-version '26.08'
x-gl-version: &gl-version '1.4'
x-gl-versions: &gl-versions 26.08;26.08-extra;1.4
x-gl-merge-dirs: &gl-merge-dirs vulkan/icd.d;glvnd/egl_vendor.d;egl/egl_external_platform.d;OpenCL/vendors;lib/dri;lib/d3d;lib/gbm;vulkan/explicit_layer.d;vulkan/implicit_layer.d
command: faugus-launcher
separate-locales: false # translations

finish-args:
  - --share=network
  - --share=ipc
  - --socket=wayland
  - --socket=x11
  - --socket=pulseaudio # notification sound
  - --filesystem=xdg-run/pipewire-0 # native pipewire audio driver
  - --allow=devel # ubisoft connect and other launchers
  - --allow=bluetooth
  - --allow=multiarch
  - --allow=per-app-dev-shm  # pressure-vessel
  - --device=all
  - --talk-name=org.kde.StatusNotifierWatcher # system tray
  - --system-talk-name=org.freedesktop.UDisks2 # show mounted devices
  - --own-name=com.steampowered.* # run multiple games in the same prefix
  - --env=PATH=/app/bin:/app/utils/bin:/usr/bin:/usr/lib/extensions/vulkan/MangoHud/bin:/usr/lib/extensions/vulkan/gamescope/bin:/usr/lib/extensions/vulkan/OBSVkCapture/bin # mangohud, gamescope, obsvkcapture
  - --filesystem=home
  - --filesystem=/mnt
  - --filesystem=/run/media
  - --filesystem=xdg-config/MangoHud:ro

add-extensions:
  org.freedesktop.Platform.Compat.i386:
    directory: lib/i386-linux-gnu
    version: *fd-version

  org.freedesktop.Platform.GL32:
    directory: lib/i386-linux-gnu/GL
    version: *gl-version
    versions: *gl-versions
    subdirectories: true
    no-autodownload: true
    autodelete: false
    add-ld-path: lib
    merge-dirs: *gl-merge-dirs
    download-if: active-gl-driver
    enable-if: active-gl-driver
    autoprune-unless: active-gl-driver

  org.freedesktop.Platform.VAAPI.Intel.i386:
    directory: lib/i386-linux-gnu/dri/intel-vaapi-driver
    version: *fd-version
    versions: *fd-version
    autodelete: false
    no-autodownload: true
    add-ld-path: lib
    download-if: have-intel-gpu
    autoprune-unless: have-intel-gpu

modules:
  - name: bundle-setup
    buildsystem: simple
    build-commands:
      - mkdir -p /app/lib/i386-linux-gnu
      - mkdir -p /app/lib/debug/lib/i386-linux-gnu
      - mkdir -p /app/lib/i386-linux-gnu/GL
      - mkdir -p /app/lib/i386-linux-gnu/dri/intel-vaapi-driver
      - install -Dm644 ld.so.conf /app/etc/ld.so.conf
    sources:
      - type: inline
        dest-filename: ld.so.conf
        contents: |
          /app/lib/i386-linux-gnu

  - name: faugus-launcher
    builddir: true
    buildsystem: meson
    build-options:
      config-opts:
        - -Dflatpak_build=true
    sources:
      - type: archive
        url: https://github.com/Faugus/faugus-launcher/archive/refs/tags/2.4.1.tar.gz
        sha256: c3676baeaab8ff7f88b0272a206e17ea258a85f4369d7b06e16040a47cc5ec26

    modules:
      - modules/python3-dbus.json
      - modules/python3-icoextract.json
      - modules/python3-requests.json
      - modules/python3-psutil.json
      - modules/python3-vdf.json
      - modules/python3-Pillow.json
